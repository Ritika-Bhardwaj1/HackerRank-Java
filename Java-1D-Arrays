import java.util.*;

public class Solution {

    public static void main(String[] args) {
	   
        Scanner scan = new Scanner(System.in);
        int n = scan.nextInt();
        int a[] = new int [n]; //initialising an array of size n
        for (int i =0; i<n; i++){ //runs on the no. of elements till n  not from 0 to n-1 
            int val = scan.nextInt();
            a[i] = val; //assign val to a[i] and siplaying a[i]
        }
        scan.close();

        // Prints each sequential element in array a
        for (int i = 0; i < a.length; i++) {
            System.out.println(a[i]);
        }
    }
}
